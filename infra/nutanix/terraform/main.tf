// Nutanix Terraform skeleton for on-premises deployments.
// Replace placeholders and uncomment the provider block when ready.

// Example provider (requires the Nutanix provider to be installed):
/*
provider "nutanix" {
  # Configure the Nutanix provider with your Prism details
  endpoint = var.prism_endpoint
  username = var.prism_username
  password = var.prism_password
  insecure = true // set to false when using proper TLS certs
}
*/

// Example resource/module usage goes here. Keep this file minimal as a
// starting point for your specific Nutanix resources (VMs, networks, images).

/*
module "example_vm" {
  source = "./modules/vm"
  # module inputs
}
*/
