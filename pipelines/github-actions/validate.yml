name: Validate IaC

on:
  pull_request:
    branches: [ "main" ]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Format Check (DaaS)
        working-directory: ./infra/workloads/daas/terraform
        run: |
          terraform fmt -check -diff

      - name: Install TFLint
        run: |
          curl -sSfL https://raw.githubusercontent.com/terraform-linters/tflint/master/install_linux.sh | bash

      - name: TFLint Init & Run (DaaS)
        working-directory: ./infra/workloads/daas/terraform
        run: |
          tflint --init || true
          tflint || true

      - name: tfsec Scan (DaaS)
        uses: aquasecurity/tfsec-action@v1
        with:
          directory: ./infra/workloads/daas/terraform

      - name: Terraform Validate (DaaS)
        working-directory: ./infra/workloads/daas/terraform
        run: |
          terraform init -backend=false
          terraform validate

      - name: Terraform Format Check (NetScaler)
        working-directory: ./infra/workloads/netscaler/terraform
        run: |
          terraform fmt -check -diff

      - name: TFLint Init & Run (NetScaler)
        working-directory: ./infra/workloads/netscaler/terraform
        run: |
          tflint --init || true
          tflint || true

      - name: tfsec Scan (NetScaler)
        uses: aquasecurity/tfsec-action@v1
        with:
          directory: ./infra/workloads/netscaler/terraform

      - name: Terraform Validate (NetScaler)
        working-directory: ./infra/workloads/netscaler/terraform
        run: |
          terraform init -backend=false
          terraform validate
